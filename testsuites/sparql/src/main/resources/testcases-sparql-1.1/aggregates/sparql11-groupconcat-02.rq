PREFIX : <http://example/>
SELECT (GROUP_CONCAT(?name) AS ?names)
WHERE {
  { 
    SELECT ?org ?name 
    WHERE {
  		?org :affiliates ?auth .
  		?auth :name ?name. 
  	}
  	GROUP BY ?org
   	ORDER BY DESC(str(?name))
  }
}
